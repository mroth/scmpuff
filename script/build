#!/usr/bin/env bash
# Usage: build
#
# Run go build with dependencies
#
# Author: Jingwen Owen Ou
# Modified: Matthew Rothenberg

if [ ! -x $GOPATH/bin/go-bindata ]; then
  script/bootstrap
fi

go generate ./...
go build -o bin/scmpuff
